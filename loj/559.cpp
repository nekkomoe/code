#include <bits/stdc++.h>
using namespace std;
int x;
int main() {
    while(1) {
        puts("reach_dest");
        fflush(stdout);
        scanf("%d", &x);
        puts("move_left");
        fflush(stdout);
        scanf("%d", &x);
    }
}
